plugins {
    id 'com.android.application'
}

android {
    namespace 'com.reveny.imguimod'
    compileSdk 34

    defaultConfig {
        applicationId "com.reveny.imguimod"
        minSdk 21
        targetSdk 34
        versionCode 1
        versionName "1.0"
    }

    buildTypes {
        release {
            minifyEnabled false
            proguardFiles getDefaultProguardFile('proguard-android-optimize.txt'), 'proguard-rules.pro'
        }
        debug {
            minifyEnabled false
        }
    }

    externalNativeBuild {
        cmake {
            cppFlags "-std=c++17 -fPIC"
        }
    }

    ndk {
        abiFilters 'arm64-v8a'
    }
}

dependencies {
    implementation fileTree(dir: 'libs', include: ['*.jar'])
}
